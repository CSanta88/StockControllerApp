<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import java.net.URL?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.stockcontroller.controller.view.ArticuloViewController"
            prefWidth="800.0" prefHeight="600.0">

    <stylesheets>
        <URL value="@../css/estilos.css"/>
    </stylesheets>

    <top>
        <VBox spacing="10">
            <padding>
                <Insets top="15" right="15" bottom="15" left="15"/>
            </padding>
            <HBox spacing="10">
                <Button fx:id="btnVolver" text="Volver al Dashboard" styleClass="btn-secundario"/>
                <Label text="Gestión de Artículos" style="-fx-font-size: 20px; -fx-font-weight: bold;" />
            </HBox>
            <HBox spacing="10">
                <TextField fx:id="campoBusqueda" promptText="Buscar por nombre..." />
                <Button text="Buscar" onAction="#buscarPorNombre" styleClass="btn-primario"/>
                <Button text="Stock Bajo" onAction="#filtrarStockBajo" styleClass="btn-secundario"/>
                <Button text="Mostrar Todos" onAction="#cargarArticulos" styleClass="btn-secundario"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <VBox spacing="15">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

            <TableView fx:id="tablaArticulos">
                <columns>
                    <TableColumn fx:id="columnaNombre" text="Nombre"/>
                    <TableColumn fx:id="columnaDescripcion" text="Descripción"/>
                    <TableColumn fx:id="columnaPrecio" text="Precio"/>
                    <TableColumn fx:id="columnaStock" text="Stock"/>
                </columns>
            </TableView>

            <GridPane hgap="10" vgap="10">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>

                <Label text="Nombre:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
                <TextField fx:id="campoNombre" GridPane.rowIndex="0" GridPane.columnIndex="1"/>

                <Label text="Descripción:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
                <TextField fx:id="campoDescripcion" GridPane.rowIndex="1" GridPane.columnIndex="1"/>

                <Label text="Precio:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
                <TextField fx:id="campoPrecio" GridPane.rowIndex="2" GridPane.columnIndex="1"/>

                <Label text="Stock:" GridPane.rowIndex="3" GridPane.columnIndex="0"/>
                <TextField fx:id="campoStock" GridPane.rowIndex="3" GridPane.columnIndex="1"/>
            </GridPane>

            <HBox spacing="10">
                <Button text="Crear" onAction="#crearArticulo" styleClass="btn-crear"/>
                <Button text="Actualizar" onAction="#actualizarArticulo" styleClass="btn-actualizar"/>
                <Button text="Eliminar" onAction="#eliminarArticulo" styleClass="btn-eliminar"/>
            </HBox>
        </VBox>
    </center>
</BorderPane>
